version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/app/deploy
    overwrite: yes

hooks:
  BeforeInstall:
    - location: scripts/dev_beforeInstall.sh
      runas: root
  AfterInstall:
    - location: scripts/dev_afterInstall.sh
      runas: root